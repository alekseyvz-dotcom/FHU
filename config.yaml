telegram:
  token: "YOUR_TELEGRAM_BOT_TOKEN"
  chat_id: 123456789

storage:
  excel_path: "data/incidents.xlsx"  # локальный реестр инцидентов (создастся автоматически)

ui:
  # Предзаполненный "Дежурный" (можно оставить пустым)
  default_duty: ""
